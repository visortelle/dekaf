apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "_chart-name.fullname" . }}-test-connection"
  labels:
    {{- include "_chart-name.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "_chart-name.fullname" . }}:{{ .Values.dekaf.service.port }}']
  restartPolicy: Never
