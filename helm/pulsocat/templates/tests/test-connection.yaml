apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pulsocat.fullname" . }}-test-connection"
  labels:
    {{- include "pulsocat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pulsocat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
