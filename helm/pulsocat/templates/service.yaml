apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulsocat.fullname" . }}
  labels:
    {{- include "pulsocat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pulsocat.selectorLabels" . | nindent 4 }}
