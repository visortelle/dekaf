apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pulsocat-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pulsocat-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pulsocat-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
