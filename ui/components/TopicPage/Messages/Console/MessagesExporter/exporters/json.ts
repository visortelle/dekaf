import { MessageDescriptor } from "../../../types";
import { ExportConfig } from "../types";

type CreateJsonFileContentProps = {
  messages: MessageDescriptor;
  config: ExportConfig;
};

export function createJsonFileContent(
  props: CreateJsonFileContentProps
): Uint8Array {
  
}
